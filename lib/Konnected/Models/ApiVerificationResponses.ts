'use strict';

export const enum ApiVerificationResponses {
  Success = 'success',
  AuthenticationRequired = 'authentication_required',
  InvalidCredentials = 'invalid_credentials',
}

export default ApiVerificationResponses;
